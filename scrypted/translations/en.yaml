---
configuration:
  env_vars:
    name: Scrypted environment variables
    description: A list of environment variables that you want to set. Only accepts environment variables that start with "SCRYPTED_". 